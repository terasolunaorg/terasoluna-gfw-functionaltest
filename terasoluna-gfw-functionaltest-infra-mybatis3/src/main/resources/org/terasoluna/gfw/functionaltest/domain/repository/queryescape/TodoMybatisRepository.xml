<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
    namespace="org.terasoluna.gfw.functionaltest.domain.repository.queryescape.TodoMybatisRepository">

    <resultMap id="todo"
        type="org.terasoluna.gfw.functionaltest.domain.model.Todo">
        <result property="id" column="id" />
        <result property="todoTitle" column="todo_title" />
    </resultMap>

    <select id="findAllByTitleLike" parameterType="java.lang.String" resultMap="todo">
    <![CDATA[
        SELECT
            id, 
            todo_title
        FROM
            todo 
        WHERE
            todo_title LIKE #{todoTitle} ESCAPE '~'
        ORDER BY
            id
    ]]></select>
</mapper>