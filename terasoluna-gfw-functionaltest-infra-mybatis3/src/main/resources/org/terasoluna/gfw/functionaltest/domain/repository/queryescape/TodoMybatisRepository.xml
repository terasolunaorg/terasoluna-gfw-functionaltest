<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
    namespace="org.terasoluna.gfw.functionaltest.domain.repository.queryescape.TodoMybatisRepository">

    <select id="findAllByTitleLike" parameterType="string" resultType="Todo">
    <![CDATA[
        SELECT
            id, 
            todo_title
        FROM
            todo 
        WHERE
            todo_title LIKE #{todoTitle} ESCAPE '~'
        ORDER BY
            id
    ]]>
    </select>
</mapper>