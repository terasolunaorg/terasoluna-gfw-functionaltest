<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
    namespace="org.terasoluna.gfw.functionaltest.domain.repository.queryescape.VersionInfoMybatisRepository">

    <select id="findOne" resultType="string">
        <![CDATA[
            SELECT version FROM PRODUCT_COMPONENT_VERSION WHERE product LIKE 'Oracle%'
        ]]>
    </select>

</mapper>